# frontend — HuntAI SRE 前端

前端源码与前端工程配置。禁止放设计文档或后端代码。

当前阶段（Stage 2）本目录只保留本文件。实现代码于 Stage 11 落地，目录结构按 Stage 6 `docs/06_architecture_design/frontend_design_spec-v1.0.md` 创建；该文件不存在时禁止预建模块目录。

构建与运行命令在 Stage 11 写入本文件。配置从仓库根 `.env` 注入，键名见根 `.env.example`。
