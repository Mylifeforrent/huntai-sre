# 07_backend_design — 数据模型与 API 规范

存放数据模型与 API 规范产出：逻辑表 / ER / 字段分级，以及 API 契约与 Gate 2 记录。
由数据模型与 API 规范阶段产出。

**约定输出**：

- `data_model_spec.md` — 逻辑数据模型（Markdown）
- `api_interface_spec.md` — API 契约（Markdown）
- `gate2_review.md` — Gate 2 记录（Markdown）
